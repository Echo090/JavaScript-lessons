let path = require("path");

const resolvedPath = path.resolve("folder1", "2.txt");
console.log(resolvedPath);
const joinedPath = path.join("folder1", "2.txt");
console.log(joinedPath);
console.log("ey");
